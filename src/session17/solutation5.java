package session17;

public class solutation5 {
    /*A HashMap stores data in the form of key-value pairs, similar to how a database table stores records using a unique identifier.

For example, in a Zomato order history system, the order ID can be used as the key, while order details such as restaurant name, amount, and order status can be stored as the value.

This structure allows a program to quickly search and access the details of a specific order using its unique order ID. */
}
